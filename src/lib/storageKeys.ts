/** localStorage key for coverage + mastery (must match useCoverageStore). */
export const COVERAGE_STORAGE_KEY_V2 = 'ai-life-coverage-v2'

/** sessionStorage snapshot before last confirmed import (undo). */
export const IMPORT_UNDO_SESSION_KEY = 'ai-life-import-undo'
